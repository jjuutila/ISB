%header
  - if @partition and @statistics.size > 0
    %h1= "Pistepörssi kaudella #{@partition.season}, #{@partition.name.downcase}"
  - else
    %h1= "Pistepörssi ei ole saatavilla"

= render :partial => 'section/statistics', :locals => { :statistics => @statistics }