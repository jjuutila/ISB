%h1 Ajankohtaista
%p= link_to 'Luo uusi uutinen', new_admin_news_path
#notice= notice
%table
  %thead
    %tr
      %th{:colspan => 3}= 'Uutiset'
  
  %tbody
    - for news_post in @news
      %tr{ :class => cycle(:odd, :even) }

        %td= news_post
        %td.action= link_to 'Muokkaa', edit_admin_news_path(news_post)
        %td.action= link_to 'Poista', admin_news_path(news_post), :confirm => 'Oletko varma?', :method => :delete
